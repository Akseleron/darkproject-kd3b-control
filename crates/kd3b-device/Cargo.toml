[package]
name = "kd3b-device"
version = "0.1.0"
publish = false
edition.workspace = true
rust-version.workspace = true

[dependencies]
kd3b-protocol = { path = "../kd3b-protocol" }

[target.'cfg(target_os = "linux")'.dependencies]
hidapi = { version = "=2.6.6", default-features = false, features = ["linux-static-libusb"] }

[lints]
workspace = true
